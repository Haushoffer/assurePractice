---->how to deply my mysql container
    docker run --name some-mysql -e MYSQL_ROOT_PASSWORD=my-secret-pw -d mysql:tag
--->how to access to mysql container
    docker container ls ----choose te mysql one
    docker exec -it <container id> bash
mysql -u root -p
mysql> GRANT ALL PRIVILEGES ON *.* TO 'mauri'@'localhost' IDENTIFIED BY '123456';




en que db estoy SELECT DATABASE();